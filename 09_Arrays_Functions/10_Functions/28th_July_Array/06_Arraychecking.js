let fruit = ["Banana", "Mango", "Pom"];
Array.isArray(fruit); // check if fruit is an array
console.log(Array.isArray(fruit)); // Output: true

let scrores = [10, 20, 30];
console.log(scrores.every((value) => value > 5)); // Output: true